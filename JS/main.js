function surface()
        {
            // var entre = document.getElementById('nanim').value;
            // var name = document.getElementsByClassName('nom')[1].value;
            // var elements = document.getElementsByClassName('className');

            // Obtenir un handle sur un élément du DOM via son attribut id unique
            // var element = document.getElementById("9");

            // obtient un tableau contenant touts les éléments portant cette classe
            
            // var elements = document.getElementsByClassName('nom');

            // var elements = [];

            // this.elements.forEach(element => {
            //     if (entre == element) {
            //         alert("Match")
            //     }else {
            //         alert("No match")
            //     }
            // });

            // obtien un ou plusieurs éléments 
            // var element = document.querySelector("comme le css");



            // surligné =>
            // prendre le texte innerText
            // remplace la partie recherchée par
            // <span class="highlight">mot</span>
        }  